# Eunoma DeOperator V2 env vars (example)
# Copy to .env for local development. Never commit real values.

# ===== Aptos network =====
APTOS_NETWORK=testnet
APTOS_NODE_URL=https://fullnode.testnet.aptoslabs.com/v1

# ===== Bridge contract (Eunoma testnet V4 active deploy — chain-verified 2026-06-06) =====
# These addresses match the operator cluster state (slot-N/vault_state_v2.json).
# To re-verify after a redeploy, read the on-chain `<pkg>::eunoma_bridge::VaultCoreV4`
# and AssetRegistryV4 resources. V4 clean deployments may not have legacy BridgeVault.
# 历史部署 (dormant / DO NOT USE)：
#   v2.1 / A6    2026-05-24 = 0xa08850b1... / 0xbbb0957e...
#   v2.0 pre-A6  2026-05-19 = 0xa08850b1... / 0x554cd51d...   (decommissioned after P0 fix; do not redeploy)
#   blocker1_l4 2026-05-12 = 0x5bf0ecca... / 0xe71cca7c...   (no BridgeVault on chain)
#   W6.4        2026-05-10 = 0x8268f56b... / 0x9545e5ea...
#   AptosShield            = 0x7825166d... / 0xe596a6ca...
BRIDGE_PACKAGE_ADDRESS=0xacb0f899d4ba3809d95022e873281d019cb138ba0a242b95426983ae2da0ae51
# Vault resource account = account::create_resource_account(admin, vault_seed) — distinct from the
# package/admin address and from any tx sender. VAULT_RESOURCE_ADDRESS and BRIDGE_VAULT_ADDRESS are
# ALIASES for the SAME on-chain vault address; keep them identical. Backend reads BRIDGE_VAULT_ADDRESS
# (coordinator/src/config.ts); VAULT_RESOURCE_ADDRESS is a legacy/doc alias (not read by current code).
VAULT_RESOURCE_ADDRESS=0x9415c478cad6d98036119f22d2b87e91a9236c42dca4df105b7b9acfcf688ee4
BRIDGE_VAULT_ADDRESS=0x9415c478cad6d98036119f22d2b87e91a9236c42dca4df105b7b9acfcf688ee4
# Confidential-asset metadata object address (0xa = APT fungible-asset metadata on Aptos).
BRIDGE_ASSET_TYPE=0xa

# ===== Shared V2 roster =====
# JSON-encoded DeoperatorRoster from @eunoma/deop-protocol.
# Must contain exactly seven nodes and threshold 5.
DEOPERATOR_ROSTER_JSON=

# Optional Postgres DSNs for persistent V2 metadata stores.
COORDINATOR_DATABASE_URL=
DEOPERATOR_DATABASE_URL=

# ===== Coordinator =====
COORDINATOR_HOST=127.0.0.1
COORDINATOR_PORT=4200
COORDINATOR_BEARER_TOKEN=
# JSON object mapping node slot strings ("0".."6") to bearer tokens.
# These are used for coordinator-to-node forwarding and are not published in the roster.
DEOPERATOR_NODE_BEARER_TOKENS_JSON=

# ===== Deoperator node =====
DEOPERATOR_SLOT=0
DEOPERATOR_NODE_ID=node-0
DEOPERATOR_HOST=127.0.0.1
DEOPERATOR_PORT=4100
DEOPERATOR_BEARER_TOKEN=

# Rust worker endpoint for DKG/FROST/MPC work.
# If unset, TypeScript services fail closed.
CRYPTO_WORKER_URL=http://127.0.0.1:4400
CRYPTO_WORKER_SLOT=0
CRYPTO_WORKER_HOST=127.0.0.1
CRYPTO_WORKER_PORT=4400
CRYPTO_WORKER_STATE_ROOT=.agent-local/eunoma-v2-asp
CRYPTO_WORKER_STATE_DIR=.agent-local/eunoma-v2-asp/slot-0

# Required by `npm run local:cluster:config`.
# Must come from Aptos-compatible threshold CA DKG/registration tooling.
# The local runner intentionally refuses to synthesize this value.
LOCAL_VAULT_EK_HEX=

# Vault TwistedEd25519 encryption pubkey (32 bytes hex). Public value.
VAULT_EK_HEX=0xeaf09b7b6e57f4861d4cd9d5c2cc3e2c2cf2f6bbfab3c6b5831b61a8feef7454

# Bridge-spare recipient TwistedEd25519 keypair (32 bytes hex) —— testnet 测试用收款方
RECIPIENT_ENCRYPTION_KEY_HEX=

# ===== Relayer =====
RELAYER_HOST=127.0.0.1
RELAYER_PORT=4300
RELAYER_BEARER_TOKEN=
RELAYER_USE_V3=1
RELAYER_SUBMIT_ENABLED=1
# Deposit step2b charges this flat APT fee and sends it to the reserve account.
# 4,000,000 octas covered the measured delegated deposit path on 2026-06-06.
RESERVE_ACCOUNT_ADDRESS=0x1347ad32528435dfd3bcdc49d0c2b2144d2cbefd4fa56bfe6598b56d88fee1db
EUNOMA_DEPOSIT_RELAYER_GAS_FEE_OCTAS=4000000
RELAYER_MAX_GAS_PRICE_OCTAS=1000
RELAYER_RESERVE_MIN_BALANCE_OCTAS=200000000

# ===== Admin / relayer transaction keys =====
# These are transaction submitter keys only. No full vault CA key belongs here.
ADMIN_PRIVATE_KEY_HEX=
RELAYER_PRIVATE_KEY_HEX=

# ===== Testnet rotation (FROST DKG admin tx) =====
# aptos CLI profile name with admin signer authority for rotate_deoperator_config_v2.
# Used by scripts/testnet_rotate_frost_config.mjs.
ADMIN_PROFILE=
